import { COLORS } from 'styles';

export default {
    zoomContainer: {
        backgroundColor: COLORS.white,
        width: 48,
        height: 48,
        borderRadius: '50%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
    zoomIcon: {
        flex: 1,
        color: COLORS.text,
        fontSize: '18px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
};
